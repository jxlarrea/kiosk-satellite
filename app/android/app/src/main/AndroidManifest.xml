<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- SendSpin server discovery over mDNS: receiving multicast needs the
         wifi multicast lock, which needs both of these. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Button haptics: a short click from the vibration motor on
         dashboard taps (HapticsBridge). Normal permission, no prompt. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Local Media screensaver: reading the user's picked photo/video
         folder. READ_MEDIA_* on 13+, the legacy storage grant before. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <!-- Writing (delete, upload) in the File Manager's shared root before
         Android 11, where All files access does not exist and the legacy
         storage pair is the whole grant (issue #175). The
         requestLegacyExternalStorage attribute on the application keeps
         that pair meaningful on Android 10's scoped storage; 11+ ignores
         the attribute. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>
    <!-- Motion detection uses a camera when one is present, but the app is fully
         usable without it, so never let the Store filter the install. -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Bluetooth proxy: relaying BLE advertisements to Home Assistant.
         The modern runtime pair on 12+; neverForLocation keeps the scan
         grant from dragging location in. The legacy pair below is the whole
         Bluetooth grant on 11 and older (where scanning also needs the
         location permissions already declared above, plus location services
         switched on). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <!-- Background listening. Android freezes cached processes whole, so without
         a foreground service the mic, the WebView and our own Dart all stop the
         moment another app is in front. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- Keeps motion detection's camera alive when the panel powers off.
         Newer Android revokes camera access from apps that are not visible
         unless a camera-type foreground service is running; the type is only
         attached when the CAMERA permission is granted (WakeWordService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <!-- Keeps BLE scan callbacks flowing while the app is not on screen:
         Android 14+ requires the connectedDevice type for that, and the type
         requires this permission (BluetoothProxyService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>
    <!-- The service's notification. Denied, the service still runs; it just says
         nothing, which is worse for the person in the room than for us. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Starting our Activity from behind another app on Android 10+. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- In-app updates: handing the downloaded APK to the system installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Without this, Android 12+ ignores USER_ACTION_NOT_REQUIRED and shows
         the confirm screen even for an installer-of-record self-update
         (see ApkInstaller). Normal protection: granted at install. -->
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION"/>
    <!-- The File Manager's shared-storage root. A special settings-screen
         grant ("All files access"), requested from the File Manager and
         the Permissions Manager; without it the manager still works on
         the app's own folder. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"/>
    <!-- Writing the real system brightness (the value quick-settings moves),
         so brightness set from the remote admin or Home Assistant matches
         what every other surface reports. Granted via the special "Modify
         system settings" screen, never a runtime prompt. -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions"/>
    <!-- Which app is on screen, for the MQTT "Foreground app" sensor
         (issue #192). Granted via the special "Usage access" screen, never
         a runtime prompt; without it the sensor only knows when Kiosk
         Satellite itself is in the foreground. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>
    <application
        android:label="Kiosk Satellite"
        android:name=".KioskApplication"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">
        <!-- singleTask with a real affinity: exactly one instance, ever.
             The scaffold's singleTop + empty affinity meant no launch could
             find the existing task, so the boot receiver (or a redeploy
             relaunch) rooted a second one beside it. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="me.jxl.kiosk_satellite"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <service
            android:name=".WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone|camera"/>
        <!-- The Bluetooth proxy's unfreeze exemption; the proxy itself lives
             in btproxy/BluetoothProxyRuntime. See BluetoothProxyService. -->
        <service
            android:name=".BluetoothProxyService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"/>
        <!-- Crash recovery (issue #94): the sticky restart of this empty
             service is what relaunches the kiosk after a native crash, and
             the heartbeat alarm below is its backstop on OEMs that restart
             sticky background services lazily. See CrashSelfHeal. -->
        <service
            android:name=".CrashGuardService"
            android:exported="false"/>
        <!-- The System UI guard: closes the notification shade and recents
             the moment they open while kiosk or lockdown protections hold.
             Enabled by the owner, once, in Android's Accessibility settings;
             inert until then. See KioskAccessibilityService. -->
        <service
            android:name=".KioskAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/kiosk_accessibility"/>
        </service>
        <receiver
            android:name=".CrashHeartbeatReceiver"
            android:exported="false"/>
        <!-- Start on boot (kiosk.start_on_boot; RECEIVE_BOOT_COMPLETED is
             declared above). -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Device-owner hook: `dpm set-device-owner` targets this receiver;
             holding the role turns "Disable home button" from screen pinning
             into full lock-task (see KioskLock). -->
        <receiver
            android:name=".KioskAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>
        <!-- A silent self-update (ApkInstaller) kills the process as Android
             swaps the code; this brings the kiosk back afterwards. -->
        <receiver
            android:name=".UpdateRelaunchReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <!-- In-app updates: exposes the downloaded APK (app cache, updates/)
             to the system package installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_paths"/>
        </provider>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Launching another app from an app:// link in the dashboard. Without
             this, package visibility hides every other app from us and
             getLaunchIntentForPackage returns null for all of them. Scoped to
             apps that have a launcher entry: enough to start them, and no more
             than a launcher already sees. -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>
</manifest>
